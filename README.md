# 🔐 Safe Demo — Simple Python HTTP Form Server

This project is a minimal Python-based HTTP server that displays a password-like input form and returns the submitted value on another page.  
Made for learning, demos, and experimenting with basic web request handling using Python.

---

## 🚀 Features
- Clean dark UI with HTML & CSS
- Runs a lightweight Python HTTP server
- Accepts POST form submissions
- Displays user input dynamically
- Zero external dependencies (no pip install required)

---

## 📦 Installation

### 1. Clone the Repository
```bash
>git clone https://github.com/your-username/safe-demo.git
cd safe-demo
python3 -m venv venv
source venv/bin/activate
python3 safe_demo.py
http://localhost:8000<
